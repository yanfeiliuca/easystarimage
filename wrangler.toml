name = "easystarimage-api"
main = "api/generate.js"
compatibility_date = "2025-05-23"

# 部署后设置 API Key（不写入文件）:
#   wrangler secret put EVOLINK_API_KEY

[observability]
enabled = true
head_sampling_rate = 1
